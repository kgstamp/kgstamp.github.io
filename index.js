import { useState } from 'react';

export default function CurrentPage() {
    return (
        <div>
            <body>
                <p>Hello world.</p>
            </body>
        </div>
    );
}

// ok maybe not doing react, just regular js. so ignore all this for now